<% /* skeleton: { width, height, class } */
var skWidth  = locals.width  || 'w-full';
var skHeight = locals.height || 'h-4';
var skClass  = locals.class  || '';
%>
<div class="al-skeleton <%= skWidth %> <%= skHeight %> <%= skClass %>"></div>
<% /* skeleton: { width, height, class } */
const skWidth  = locals.width  || 'w-full';
const skHeight = locals.height || 'h-4';
const skClass  = locals.class  || '';
%>
<div class="al-skeleton <%= skWidth %> <%= skHeight %> <%= skClass %>"></div>